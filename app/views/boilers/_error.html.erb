<% if @facility.boilers.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facility.boilers.errors.count, "error") %> prohibited this boiler from being saved:</h2>

      <ul>
      <% @facility.boilers.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>